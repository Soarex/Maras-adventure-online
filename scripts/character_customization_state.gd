extends Node

var CurrentHair: ReactiveProperty = ReactiveProperty.new(null)
var CurrentHead: ReactiveProperty = ReactiveProperty.new(null)
var CurrentBody: ReactiveProperty = ReactiveProperty.new(null)
var CurrentHand: ReactiveProperty = ReactiveProperty.new(null)
var CurrentFoot: ReactiveProperty = ReactiveProperty.new(null)
